---
import Layout from "../../../layouts/Layout.astro";
import weather from "../../../assets/weather.json";

const locations = [...new Set(weather.map(w => w.location))];
---

<Layout>
  <main class="max-w-5xl mx-auto py-10 px-6">
    <section class="text-center mb-10">
      <h1 class="text-3xl font-semibold text-blue-600 mb-2">
        🌎 Liste des villes (rendu statique)
      </h1>
      <p class="text-gray-600">
        Pages générées automatiquement à la build.
      </p>
    </section>

    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        locations.map(city => (
          <a
            href={`/weather/rendu-statique/${city}`}
            class="block bg-white border border-gray-200 rounded-xl p-6 text-center shadow-sm hover:shadow-md hover:-translate-y-1 transition-all duration-300"
          >
            <h2 class="text-xl font-medium text-gray-800 mb-2">{city}</h2>
            <p class="text-gray-500 text-sm">Voir le graphique de température</p>
          </a>
        ))
      }
    </div>
  </main>
</Layout>
