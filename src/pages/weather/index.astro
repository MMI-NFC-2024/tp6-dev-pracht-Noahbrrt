---
import Layout from "../../layouts/Layout.astro";
import PlotFigure from "../../components/PlotFigure.astro";
import * as Plot from "@observablehq/plot";
import weather from "../../assets/weather.json";
---

<Layout>
  <main class="max-w-5xl mx-auto py-10 px-6">
    <section class="text-center mb-10">
      <h1 class="text-3xl font-semibold text-blue-600 mb-2">🌦️ Données météorologiques</h1>
      <p class="text-gray-600">Comparaison des températures maximales par ville.</p>
    </section>

    <div class="bg-white border border-gray-200 rounded-2xl shadow-md p-6">
      <PlotFigure
        options={{
          grid: true,
          marks: [
            Plot.line(weather, {
              x: d => new Date(d.date),
              y: "temp_max",
              stroke: "location"
            })
          ],
          x: { label: "Date" },
          y: { label: "Température max (°C)" },
          color: { legend: true }
        }}
      />
    </div>
  </main>
</Layout>
