---
import Layout from "../../../layouts/Layout.astro";
import weather from "../../../assets/weather.json";

const cities = [...new Set(weather.map(w => w.location))];
---

<Layout>
  <main class="max-w-5xl mx-auto py-10 px-6 text-center">
    <h1 class="text-3xl font-semibold text-blue-600 mb-6">
      🌤️ Données météo par ville
    </h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {cities.map(city => (
        <a
          href={`/weather/rendu-dynamique/${city}`}
          class="block bg-white border border-gray-200 shadow-sm rounded-xl p-6 text-center hover:shadow-md hover:-translate-y-1 transform transition-all duration-300"
        >
          <h2 class="text-xl font-medium text-gray-800 mb-2">{city}</h2>
          <p class="text-gray-500 text-sm">Clique pour voir les graphiques météo</p>
        </a>
      ))}
    </div>
  </main>
</Layout>
