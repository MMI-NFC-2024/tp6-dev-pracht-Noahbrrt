---
import Layout from "../../layouts/Layout.astro";
import penguins from "../../assets/penguins.json";

const speciesList = [...new Set(penguins.map(p => p.species))];
---

<Layout>
  <main class="max-w-5xl mx-auto py-10 px-6">
    <section class="text-center mb-10">
      <h1 class="text-3xl font-semibold text-blue-600 mb-2">
        🐧 Liste des espèces de manchots
      </h1>
      <p class="text-gray-600">
        Pages générées statiquement à la construction du site.
        <br />
        Sélectionne une espèce ci-dessous pour afficher son graphique.
      </p>
    </section>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        speciesList.map(species => (
          <a
            href={`/rendu-statique/${species}`}
            class="block bg-white border border-gray-200 shadow-sm rounded-xl p-6 text-center hover:shadow-md hover:-translate-y-1 transform transition-all duration-300"
          >
            <h2 class="text-xl font-medium text-gray-800 mb-2">{species}</h2>
            <p class="text-gray-500 text-sm">Clique pour voir le graphique</p>
          </a>
        ))
      }
    </div>
  </main>
</Layout>

<style>
  h1, p {
    transition: all 0.2s ease-in-out;
  }

  h1:hover {
    color: #1d4ed8;
  }
</style>
