---
import PlotFigure from "../components/PlotFigure.astro";
import Layout from "../layouts/Layout.astro";
import * as Plot from "@observablehq/plot";
import penguins from "../assets/penguins.json";
---

<Layout>
  <main class="flex flex-col items-center justify-center min-h-screen bg-gradient-to-br from-gray-50 via-white to-gray-100 text-gray-800">
    <section class="w-full max-w-4xl mx-auto px-6 py-12 text-center shadow-lg rounded-2xl bg-white/80 backdrop-blur-sm border border-gray-200">
      <h1 class="text-3xl font-semibold text-gray-800 mb-4">
        Étude des Manchots – Astro × Observable Plot
      </h1>
      <p class="text-gray-600 mb-8">
        Visualisation des données issues du jeu de données <strong>Penguins</strong> :
        <br />
        comparaison de la longueur et de la profondeur du bec selon l’espèce.
      </p>

      <div class="bg-gray-50 border border-gray-200 rounded-xl p-4 shadow-inner">
        <PlotFigure
          options={{
            grid: true,
            marks: [
              Plot.dot(penguins, {
                x: "culmen_length_mm",
                y: "culmen_depth_mm",
                stroke: "species",
              }),
            ],
            x: { label: "Longueur du bec (mm)" },
            y: { label: "Profondeur du bec (mm)" },
            color: { legend: true },
          }}
        />
      </div>
    </section>
  </main>
</Layout>

<style>
  h1, p {
    transition: all 0.3s ease-in-out;
  }

  h1:hover {
    color: #2563eb;
  }
</style>
